package Learning_Records;

public class Constants {
    public static void main(String[] args) {

        /*
            CONSTANTS IN JAVA: --------------------------------------------------------------------------------
            - A constant in Java is a variable that cant be changed.
            - A constant variable is a final Variable of primitive type, or type String, that is initialized with
            a constant expression
            - Constants in Java, are usually named with all uppercase letters, and with underscores between words
            - A static constant means you access it via the type name
            - You can see it with INTEGER.MAX_VALUE, and the INTEGER.MIN_VALUE fields
         */
    }
}
